<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="clickListener"
            type="android.view.View.OnClickListener" />
    </data>

    <LinearLayout
        android:id="@+id/activity_glide"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context="com.study.yxy.demos.ui.glide.GlideActivity">

        <Button
            android:id="@+id/btn_glide_demo1"
            android:layout_width="match_parent"
            android:layout_height="@dimen/common_btn_height"
            android:gravity="center"
            android:onClick="@{clickListener}"
            android:text="基本方法使用"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/btn_glide_demo2"
            android:layout_width="match_parent"
            android:layout_height="@dimen/common_btn_height"
            android:gravity="center"
            android:onClick="@{clickListener}"
            android:text="RecyclerView中加载图片"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/btn_glide_demo3"
            android:layout_width="match_parent"
            android:layout_height="@dimen/common_btn_height"
            android:gravity="center"
            android:onClick="@{clickListener}"
            android:text="图片变换"
            android:textAllCaps="false" />
    </LinearLayout>
</layout>